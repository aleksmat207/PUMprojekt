<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.am.projektpum">

    <permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu2" />44
        <activity android:name=".MyCalendar" />
        <activity android:name=".AddEvent" />
        <activity android:name=".MyDay" />
        <activity android:name=".MyTime" />
        <activity android:name=".MyShoppingList" />
        <activity android:name=".SpeechRecognition" />
        <activity android:name=".Notes_list"></activity>
    </application>

</manifest>